##Print Characters at Even and Odd Positions Separately##

# Read number of test cases
t = int(input())

# Loop through each test case
for _ in range(t):
    s = input()
    even_chars = ''
    odd_chars = ''
    
    # Build even and odd indexed character strings
    for i in range(len(s)):
        if i % 2 == 0:
            even_chars += s[i]
        else:
            odd_chars += s[i]
    
    # Print the result
    print(even_chars, odd_chars)
